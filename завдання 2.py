import random

def get_numbers_ticket(min: int, max: int, quantity: int) -> list:
    """
    Генерує відсортований список унікальних випадкових чисел для лотереї.

    :param min: мінімальне можливе число (не менше 1)
    :param max: максимальне можливе число (не більше 1000)
    :param quantity: кількість чисел у наборі
    :return: відсортований список унікальних чисел або пустий список,
             якщо параметри некоректні
    """
    # Перевірка коректності параметрів
    if (
        min < 1 or
        max > 1000 or
        min > max or
        quantity < 1 or
        quantity > (max - min + 1)
    ):
        return []

    # Генерація унікальних випадкових чисел
    numbers = random.sample(range(min, max + 1), quantity)

    # Повертаємо відсортований список
    return sorted(numbers)
Пояснення:
random.sample() одразу гарантує унікальність чисел.
Діапазон range(min, max + 1) включає max.
Якщо параметри некоректні — функція повертає пустий список, як вимагається.
Результат завжди відсортований.
print(get_numbers_ticket(1, 49, 6))
# Наприклад: [3, 7, 14, 22, 35, 48]
